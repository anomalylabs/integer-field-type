{{ asset_add('scripts.js', 'anomaly.field_type.integer::js/input.js') }}

<input
        type="text"
        class="spinner form-control"
        value="{{ field_type.value }}"
        data-min="{{ field_type.config.min }}"
        data-max="{{ field_type.config.max }}"
        data-step="{{ field_type.config.step }}"
        name="{{ field_type.getInputName() }}">
